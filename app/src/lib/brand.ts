/**
 * O'MERGE brand ground color, for the one place it must appear as a raw
 * literal outside CSS: the `<meta name="theme-color">` tag, which cannot
 * reference a CSS custom property. Keep in sync with `--color-om-bg` in
 * styles.css.
 */
export const BRAND_THEME_COLOR = "#0d0508";
